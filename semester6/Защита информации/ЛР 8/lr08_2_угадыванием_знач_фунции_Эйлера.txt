> # Атака угадыванием значения функции Эйлера
> # Выполнила Кочеткова М.П.
> with(numtheory):
> with(RandomTools):
> 
> # Пример из текста Упр8
> p:= 149;
> q:= 127;
> 
> n := p*q; # ив
> phi_n := phi(n); # ив
> 
> p1 := (1/2)*(n - phi_n + 1 + ((n - phi_n + 1)^2 - 4*n)^(1/2));
> q1 := (1/2)*(n - phi_n + 1 - ((n - phi_n + 1)^2 - 4*n)^(1/2));
> 
> # Проверка
> is(n = p1*q1); 