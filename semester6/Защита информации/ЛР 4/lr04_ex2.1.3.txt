> # ЛР04, Задание 2.1.3
> # Кочеткова М.П., 3курс ИВТ-2.2
> 
> with(numtheory): 
> with(RandomTools):
>  
> pB := nextprime(Generate(integer(range = 3.. 3^15))); 
> qB := nextprime(Generate(integer(range = 3.. 3^15)));
> 
> nB := pB*qB; 
> PB := phi(nB);
> 
> eB := nextprime(Generate(integer(range = 3 .. 3^15))):
> while igcd(eB, PB) <> 1 do 
>      eB := nextprime(Generate(integer(range = 3 .. 3^15)));
> end do:
> eB := eB; 
> 
> dB := eB &^ (phi(PB)-1) mod PB;
> 
> pV := nextprime(Generate(integer(range = 3 .. 3^15))); 
> qV := nextprime(Generate(integer(range = 3 .. 3^15))); 
> nV := pV*qV;
> PV := phi(nV);  
> 
> eV := Generate(integer(range = 3 .. 3^15)):
> while igcd(eV, PV) <> 1 do 
>      eV := Generate(integer(range = 3 .. 3^15));
> end do: 
> eV := eV; 
> igcd(eV, PV);
> 
> dV := eV &^ (phi(PV)-1) mod PV;
> 
> m := nextprime(Generate(integer(range = 3 .. nV)));
> 
> if nV < nB then
> 	m1 := m &^ dV mod nV;
> 	m2 := m1 &^ eB mod nB;
> 	m3 := m2 &^ dB mod nB; 
> 	m4 := m3 &^ eV mod nV;
>    else 
> 	m1 := m &^ eB mod nB; 
> 	m2 := m1 &^ dV mod nV; 
> 	m4 := m3 &^ dB mod nB;
> end if;
> m - m4;
                            9550691
                            11443967
                        109297792631197
                        109297771636540
                            12787633
                        102396070561157
                            8533297
                             505867
                         4316713353499
                         4316704314336
                            4958407
                               1
                         3959674787287
                         1213529615803
                         2488384363681
                         34758359588992
                         2488384363681
                         1213529615803
                               0
;
> 
;
